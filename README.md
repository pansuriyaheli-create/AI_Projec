# AI_Projec
Build an Android application that utilizes RAG (Retrieval-Augmented Generation) to process uploaded documents. The system extracts text, generates structured summaries using AI, and enables a conversational Q&amp;A interface. 
